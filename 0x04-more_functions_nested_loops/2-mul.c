#include "main.h"

/**
 * mul - multiplies two numbers
 *
 * @a: one of the numbers to be multiplied
 *
 * @b: one of the numbers to be multipled
 *
 * Return: a * b
 */
int mul(int a, int b)
{
	return (a * b);
}
